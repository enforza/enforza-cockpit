# docs images

Screenshots used by the docs (all from enforza-cockpit running inside Cockpit).
Referenced from [getting-started.md](../getting-started.md).

- `management-rules.png` — Management tab: the policy overview with a logged
  SSH/Cockpit accept rule.
- `add-rule-drawer.png` — the Add-rule drawer (Action / Protocol / Port / Source /
  **Log matches** / Comment).
- `network-rules.png` — Network tab: forward rules with LOG + SNAT flags.
- `objects.png` — Objects tab: reusable network (CIDR) and port objects.
