# macOS
.DS_Store
._*

# Editor / IDE
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node
node_modules/

# NOTE: dist/ is intentionally tracked. The Cockpit plugin is plain static
# HTML/CSS/JS (no build step) — dist/ IS the source that deploy.sh copies to
# /usr/share/cockpit/enforza, not generated output.

# Local logs / scratch
*.log
